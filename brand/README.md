# Sercle brand assets

This folder is reserved for approved, source-quality Sercle brand assets.

The current portal mark is rendered with CSS so the foundation has no dependency on an unapproved or provisional logo file. Add only authorized master assets here; web-ready derivatives belong in `public/`.

